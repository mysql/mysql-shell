shell.connect(__uripwd + '/mysql')
session.close()

#@ session
session.help()

#@ global ? for Session[USE:session]
\? mysqlx.Session

#@ global help for Session[USE:session]
\help mysqlx.Session

#@ session.close
session.help('close')

#@ global ? for close[USE:session.close]
\? Session.close

#@ global help for close[USE:session.close]
\help Session.close

#@ session.commit
session.help('commit')

#@ global ? for commit[USE:session.commit]
\? Session.commit

#@ global help for commit[USE:session.commit]
\help Session.commit

#@ session.create_schema
session.help('create_schema')

#@ global ? for create_schema[USE:session.create_schema]
\? Session.create_schema

#@ global help for create_schema[USE:session.create_schema]
\help Session.create_schema

#@ session.current_schema
session.help('current_schema')

#@ global ? for current_schema[USE:session.current_schema]
\? Session.current_schema

#@ global help for current_schema[USE:session.current_schema]
\help Session.current_schema

#@ session.default_schema
session.help('default_schema')

#@ global ? for default_schema[USE:session.default_schema]
\? Session.default_schema

#@ global help for default_schema[USE:session.default_schema]
\help Session.default_schema

#@ session.drop_schema
session.help('drop_schema')

#@ global ? for drop_schema[USE:session.drop_schema]
\? Session.drop_schema

#@ global help for drop_schema[USE:session.drop_schema]
\help Session.drop_schema

#@ session.get_current_schema
session.help('get_current_schema')

#@ global ? for get_current_schema[USE:session.get_current_schema]
\? Session.get_current_schema

#@ global help for get_current_schema[USE:session.get_current_schema]
\help Session.get_current_schema

#@ session.get_default_schema
session.help('get_default_schema')

#@ global ? for get_default_schema[USE:session.get_default_schema]
\? Session.get_default_schema

#@ global help for get_default_schema[USE:session.get_default_schema]
\help Session.get_default_schema

#@ session.get_schema
session.help('get_schema')

#@ global ? for get_schema[USE:session.get_schema]
\? Session.get_schema

#@ global help for get_schema[USE:session.get_schema]
\help Session.get_schema

#@ session.get_schemas
session.help('get_schemas')

#@ global ? for get_schemas[USE:session.get_schemas]
\? Session.get_schemas

#@ global help for get_schemas[USE:session.get_schemas]
\help Session.get_schemas

#@ session.get_uri
session.help('get_uri')

#@ global ? for get_uri[USE:session.get_uri]
\? Session.get_uri

#@ global help for get_uri[USE:session.get_uri]
\help Session.get_uri

#@ session.help
session.help('help')

#@ global ? for help[USE:session.help]
\? Session.help

#@ global help for help[USE:session.help]
\help Session.help

#@ session.is_open
session.help('is_open')

#@ global ? for is_open[USE:session.is_open]
\? Session.is_open

#@ global help for is_open[USE:session.is_open]
\help Session.is_open

#@ session.quote_name
session.help('quote_name')

#@ global ? for quote_name[USE:session.quote_name]
\? Session.quote_name

#@ global help for quote_name[USE:session.quote_name]
\help Session.quote_name

#@ session.release_savepoint
session.help('release_savepoint')

#@ global ? for release_savepoint[USE:session.release_savepoint]
\? Session.release_savepoint

#@ global help for release_savepoint[USE:session.release_savepoint]
\help Session.release_savepoint

#@ session.rollback
session.help('rollback')

#@ global ? for rollback[USE:session.rollback]
\? Session.rollback

#@ global help for rollback[USE:session.rollback]
\help Session.rollback

#@ session.rollback_to
session.help('rollback_to')

#@ global ? for rollback_to[USE:session.rollback_to]
\? Session.rollback_to

#@ global help for rollback_to[USE:session.rollback_to]
\help Session.rollback_to

#@ session.run_sql
session.help('run_sql')

#@ global ? for run_sql[USE:session.run_sql]
\? Session.run_sql

#@ global help for run_sql[USE:session.run_sql]
\help Session.run_sql

#@ session.set_current_schema
session.help('set_current_schema')

#@ global ? for set_current_schema[USE:session.set_current_schema]
\? Session.set_current_schema

#@ global help for set_current_schema[USE:session.set_current_schema]
\help Session.set_current_schema

#@ session.set_fetch_warnings
session.help('set_fetch_warnings')

#@ global ? for set_fetch_warnings[USE:session.set_fetch_warnings]
\? Session.set_fetch_warnings

#@ global help for set_fetch_warnings[USE:session.set_fetch_warnings]
\help Session.set_fetch_warnings

#@ session.set_savepoint
session.help('set_savepoint')

#@ global ? for set_savepoint[USE:session.set_savepoint]
\? Session.set_savepoint

#@ global help for set_savepoint[USE:session.set_savepoint]
\help Session.set_savepoint

#@ session.sql
session.help('sql')

#@ global ? for sql[USE:session.sql]
\? Session.sql

#@ global help for sql[USE:session.sql]
\help Session.sql

#@ session.start_transaction
session.help('start_transaction')

#@ global ? for start_transaction[USE:session.start_transaction]
\? Session.start_transaction

#@ global help for start_transaction[USE:session.start_transaction]
\help Session.start_transaction

#@ session.uri
session.help('uri')

#@ global ? for uri[USE:session.uri]
\? Session.uri

#@ global help for uri[USE:session.uri]
\help Session.uri
