shellresult = shell.create_result()

#@ shellresult
shellresult.help()

#@ global ? for ShellResult[USE:shellresult]
\? ShellResult

#@ global help for ShellResult[USE:shellresult]
\help ShellResult

#@ shellresult.affected_items_count
shellresult.help('affected_items_count')

#@ global ? for affected_items_count[USE:shellresult.affected_items_count]
\? ShellResult.affected_items_count

#@ global help for affected_items_count[USE:shellresult.affected_items_count]
\help ShellResult.affected_items_count

#@ shellresult.column_count
shellresult.help('column_count')

#@ global ? for column_count[USE:shellresult.column_count]
\? ShellResult.column_count

#@ global help for column_count[USE:shellresult.column_count]
\help ShellResult.column_count

#@ shellresult.column_names
shellresult.help('column_names')

#@ global ? for column_names[USE:shellresult.column_names]
\? ShellResult.column_names

#@ global help for column_names[USE:shellresult.column_names]
\help ShellResult.column_names

#@ shellresult.columns
shellresult.help('columns')

#@ global ? for columns[USE:shellresult.columns]
\? ShellResult.columns

#@ global help for columns[USE:shellresult.columns]
\help ShellResult.columns

#@ shellresult.execution_time
shellresult.help('execution_time')

#@ global ? for execution_time[USE:shellresult.execution_time]
\? ShellResult.execution_time

#@ global help for execution_time[USE:shellresult.execution_time]
\help ShellResult.execution_time

#@ shellresult.fetch_all
shellresult.help('fetch_all')

#@ global ? for fetch_all[USE:shellresult.fetch_all]
\? ShellResult.fetch_all

#@ global help for fetch_all[USE:shellresult.fetch_all]
\help ShellResult.fetch_all

#@ shellresult.fetch_one
shellresult.help('fetch_one')

#@ global ? for fetch_one[USE:shellresult.fetch_one]
\? ShellResult.fetch_one

#@ global help for fetch_one[USE:shellresult.fetch_one]
\help ShellResult.fetch_one

#@ shellresult.get_affected_items_count
shellresult.help('get_affected_items_count')

#@ global ? for get_affected_items_count[USE:shellresult.get_affected_items_count]
\? ShellResult.get_affected_items_count

#@ global help for get_affected_items_count[USE:shellresult.get_affected_items_count]
\help ShellResult.get_affected_items_count

#@ shellresult.get_column_count
shellresult.help('get_column_count')

#@ global ? for get_column_count[USE:shellresult.get_column_count]
\? ShellResult.get_column_count

#@ global help for get_column_count[USE:shellresult.get_column_count]
\help ShellResult.get_column_count

#@ shellresult.get_column_names
shellresult.help('get_column_names')

#@ global ? for get_column_names[USE:shellresult.get_column_names]
\? ShellResult.get_column_names

#@ global help for get_column_names[USE:shellresult.get_column_names]
\help ShellResult.get_column_names

#@ shellresult.get_columns
shellresult.help('get_columns')

#@ global ? for get_columns[USE:shellresult.get_columns]
\? ShellResult.get_columns

#@ global help for get_columns[USE:shellresult.get_columns]
\help ShellResult.get_columns

#@ shellresult.get_execution_time
shellresult.help('get_execution_time')

#@ global ? for get_execution_time[USE:shellresult.get_execution_time]
\? ShellResult.get_execution_time

#@ global help for get_execution_time[USE:shellresult.get_execution_time]
\help ShellResult.get_execution_time

#@ shellresult.get_info
shellresult.help('get_info')

#@ global ? for get_info[USE:shellresult.get_info]
\? ShellResult.get_info

#@ global help for get_info[USE:shellresult.get_info]
\help ShellResult.get_info

#@ shellresult.get_warnings_count
shellresult.help('get_warnings_count')

#@ global ? for get_warnings_count[USE:shellresult.get_warnings_count]
\? ShellResult.get_warnings_count

#@ global help for get_warnings_count[USE:shellresult.get_warnings_count]
\help ShellResult.get_warnings_count

#@ shellresult.get_warnings
shellresult.help('get_warnings')

#@ global ? for get_warnings[USE:shellresult.get_warnings]
\? ShellResult.get_warnings

#@ global help for get_warnings[USE:shellresult.get_warnings]
\help ShellResult.get_warnings

#@ shellresult.has_data
shellresult.help('has_data')

#@ global ? for has_data[USE:shellresult.has_data]
\? ShellResult.has_data

#@ global help for has_data[USE:shellresult.has_data]
\help ShellResult.has_data

#@ shellresult.help
shellresult.help('help')

#@ global ? for help[USE:shellresult.help]
\? ShellResult.help

#@ global help for help[USE:shellresult.help]
\help ShellResult.help

#@ shellresult.info
shellresult.help('info')

#@ global ? for info[USE:shellresult.info]
\? ShellResult.info

#@ global help for info[USE:shellresult.info]
\help ShellResult.info

#@ shellresult.next_result
shellresult.help('next_result')

#@ global ? for next_result[USE:shellresult.next_result]
\? ShellResult.next_result

#@ global help for next_result[USE:shellresult.next_result]
\help ShellResult.next_result

#@ shellresult.warnings_count
shellresult.help('warnings_count')

#@ global ? for warnings_count[USE:shellresult.warnings_count]
\? ShellResult.warnings_count

#@ global help for warnings_count[USE:shellresult.warnings_count]
\help ShellResult.warnings_count

#@ shellresult.warnings
shellresult.help('warnings')

#@ global ? for warnings[USE:shellresult.warnings]
\? ShellResult.warnings

#@ global help for warnings[USE:shellresult.warnings]
\help ShellResult.warnings
